import bcrypt from 'bcryptjs';import {query} from './db';import dotenv from 'dotenv';dotenv.config();
(async()=>{const password=await bcrypt.hash('Admin@123',10);for(const u of [['Admin User','admin@example.com','Admin'],['Sales User','sales@example.com','Sales'],['Warehouse User','warehouse@example.com','Warehouse'],['Accounts User','accounts@example.com','Accounts']] as const) await query('INSERT INTO users(name,email,password_hash,role) VALUES($1,$2,$3,$4) ON CONFLICT(email) DO NOTHING',[u[0],u[1],password,u[2]]);console.log('Seed complete. Password for all demo users: Admin@123');process.exit(0)})().catch(e=>{console.error(e);process.exit(1)});
