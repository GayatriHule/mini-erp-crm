import {Link,Outlet,useLocation} from 'react-router-dom';import {logout} from '../lib/api';
export default function Layout(){const loc=useLocation();const u=JSON.parse(localStorage.getItem('user')||'{}');return <div className="app"><aside><h2>Mini ERP</h2><p className="role">{u.role}</p><nav>{[['/','Dashboard'],['/customers','Customers'],['/products','Products'],['/challans','Sales Challans']].map(([p,n])=><Link className={loc.pathname===p?'active':''} to={p} key={p}>{n}</Link>)}</nav><button onClick={logout}>Logout</button></aside><main><header><span>Operations Portal</span><strong>{u.name}</strong></header><Outlet/></main></div>}
